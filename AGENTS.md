# AGENTS.md

本文件用于约束大模型在本仓库中的回复、代码生成与代码修改行为。所有自动化助手、编码 Agent 和大模型工具在处理本仓库任务时都必须遵守以下规则。

## 适用范围

- 本文件位于仓库根目录，规则适用于整个仓库。
- 如果子目录中存在更近层级的 `AGENTS.MD` 或 `AGENTS.md`，该子目录内的任务优先遵守更近层级文件中的规则。
- 如果用户在当前对话中给出明确指令，优先遵守用户最新的明确指令；但除非用户明确要求修改本文件，否则不要擅自放宽本文中的质量约束。

## 回复语言

- AI 的所有回复必须使用中文。
- 代码、命令、日志、错误信息、API 名称、文件路径和专有名词可以保留原文。
- 如果引用外部英文资料，必须使用中文解释其含义和结论。

## 通用代码要求

- 修改代码前，先理解现有项目结构、框架、命名和代码风格。
- 优先沿用仓库已有的模式、工具链和目录组织。
- 不做与当前任务无关的重构、格式化或依赖变更。
- 不删除或回滚用户已有改动，除非用户明确要求。
- 新增逻辑应尽量小而清晰，避免把多个职责塞进同一个方法或组件。

## JavaScript / TypeScript / Java 代码规范

- 所有 `if`、`else if`、`else` 后面的代码块都必须显式使用花括号 `{}`，不能省略。

正确示例：

```ts
if (enabled) {
  runTask();
} else {
  skipTask();
}
```

错误示例：

```ts
if (enabled) runTask();
else skipTask();
```

- 生成或修改 JavaScript、TypeScript、Java 代码时，单个方法或函数的认知复杂度不能超过 `12`。
- 如果某个方法或函数的复杂度可能超过 `12`，必须先拆分逻辑，再提交实现。
- 降低复杂度时，优先使用以下方式：
  - 提取命名清晰的辅助方法或私有方法。
  - 使用提前返回减少嵌套层级。
  - 将复杂条件提取为语义明确的布尔变量或判断方法。
  - 将重复分支合并为配置表、映射表或策略函数。
  - 避免在单个方法中混合校验、转换、业务处理和副作用操作。

## SonarQube 约束

- 所有新增或修改的 JavaScript、TypeScript、Java 方法都必须以通过 SonarQube 扫描为目标。
- 不得引入明显会触发 SonarQube 问题的代码，包括但不限于：
  - 方法复杂度超过 `12`。
  - 未使用的变量、导入、参数或私有方法。
  - 重复字符串常量。
  - 空的 `catch` 代码块。
  - 过深的嵌套条件。
  - 可以合并但被人为拆散的重复分支。
- 如果无法在本地运行 SonarQube，必须通过测试、Lint 或人工检查尽量验证上述约束。

## 测试与验证

- 修改代码后，应运行与改动范围匹配的测试、Lint 或构建命令。
- 如果因为环境、依赖或时间限制无法运行验证命令，回复中必须说明原因。
- 回复中不要声称“已通过”某项验证，除非确实运行并确认成功。

## 输出说明

- 最终回复应简洁说明完成了什么、修改了哪些文件、执行了哪些验证。
- 如果存在未验证项或风险点，必须明确说明。
